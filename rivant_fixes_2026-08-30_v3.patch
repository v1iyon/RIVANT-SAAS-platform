diff --git a/app/api/auth/google-ads/finish/route.js b/app/api/auth/google-ads/finish/route.js
index 5189440..b6c7044 100644
--- a/app/api/auth/google-ads/finish/route.js
+++ b/app/api/auth/google-ads/finish/route.js
@@ -10,6 +10,7 @@
 import { NextResponse } from "next/server";
 import { decrypt } from "@/lib/crypto";
 import { finalizeGoogleAdsConnection } from "@/lib/google-ads-connect";
+import { requireUser, UnauthorizedError, unauthorizedResponse } from "@/lib/require-user";
 
 export const runtime = "nodejs";
 
@@ -18,6 +19,22 @@ const MAX_AGE_MS = 10 * 60 * 1000;
 
 export async function POST(req) {
   try {
+    // ФІКС (аудит 30.08.2026, знахідка №5): раніше email бралось напряму з
+    // cookie gads_pending без звірки з поточною сесією — єдине місце в
+    // додатку, де requireUser() не був останнім рубежем перед записом у БД
+    // (start/callback вже роблять це правильно). Реальна експлуатованість
+    // низька (cookie httpOnly, скоуплена на домен, живе 10 хв), але для
+    // консистентності з рештою коду звіряємо, що залогінений зараз
+    // користувач — це той самий email, що ініціював підключення.
+    let currentEmail;
+    try {
+      const user = await requireUser();
+      currentEmail = user.email;
+    } catch (err) {
+      if (err instanceof UnauthorizedError) return unauthorizedResponse();
+      throw err;
+    }
+
     const { customerId } = await req.json();
     if (!customerId) {
       return Response.json({ error: "customerId is required" }, { status: 400 });
@@ -33,6 +50,10 @@ export async function POST(req) {
       return Response.json({ error: "Expired — try connecting again" }, { status: 410 });
     }
 
+    if (data.email !== currentEmail) {
+      return unauthorizedResponse();
+    }
+
     const chosen = (data.accounts || []).find((a) => a.customerId === customerId);
     if (!chosen) {
       return Response.json({ error: "That account isn't in your accessible accounts" }, { status: 400 });
diff --git a/app/api/connect-integration/route.js b/app/api/connect-integration/route.js
index f4f71bf..6c8b623 100644
--- a/app/api/connect-integration/route.js
+++ b/app/api/connect-integration/route.js
@@ -231,6 +231,25 @@ export async function DELETE(req) {
       .eq("business_id", business.id)
       .eq("provider", provider);
 
+    // ФІКС (обговорення 30.08.2026, той самий принцип, що і в
+    // /api/stripe-disconnect): резолвимо алерти цього провайдера, щоб вони
+    // не висіли "open" назавжди після відключення й не потрапляли в
+    // майбутні звіти/Risks tab про джерело, якого вже немає.
+    const ALERT_TYPE_BY_PROVIDER = {
+      meta_ads: ["sync_failure_meta_ads"],
+      google_ads: ["sync_failure_google_ads"],
+      shopify: ["sync_failure_shopify"],
+    };
+    const alertTypes = ALERT_TYPE_BY_PROVIDER[provider];
+    if (alertTypes) {
+      await admin
+        .from("alerts_log")
+        .update({ status: "resolved" })
+        .eq("business_id", business.id)
+        .eq("status", "open")
+        .in("type", alertTypes);
+    }
+
     return Response.json({ success: true });
   } catch (err) {
     if (err instanceof UnauthorizedError) return Response.json({ error: "unauthorized" }, { status: 401 });
diff --git a/app/api/stripe-disconnect/route.js b/app/api/stripe-disconnect/route.js
index bc6463e..570e6bc 100644
--- a/app/api/stripe-disconnect/route.js
+++ b/app/api/stripe-disconnect/route.js
@@ -52,6 +52,19 @@ export async function POST(req) {
       .eq("business_id", business.id)
       .eq("provider", "stripe");
 
+    // ФІКС (обговорення 30.08.2026): раніше при disconnect алерти, пов'язані
+    // з цим провайдером (sync_failure_stripe, revenue_drop, payment_silence_stripe),
+    // лишались зі status "open" НАЗАВЖДИ — жоден роут ніколи їх не резолвив,
+    // окрім повного видалення акаунта. Клієнт, який давно відключив Stripe,
+    // продовжував бачити "N alert(s) open" у щоденних звітах і на вкладці
+    // Risks — про джерело даних, якого вже нема.
+    await admin
+      .from("alerts_log")
+      .update({ status: "resolved" })
+      .eq("business_id", business.id)
+      .eq("status", "open")
+      .in("type", ["sync_failure_stripe", "revenue_drop", "payment_silence_stripe"]);
+
     return Response.json({ success: true });
   } catch (err) {
     if (err instanceof UnauthorizedError) return Response.json({ error: "unauthorized" }, { status: 401 });
diff --git a/app/api/webhooks/paddle/route.js b/app/api/webhooks/paddle/route.js
index 61165ca..e89f714 100644
--- a/app/api/webhooks/paddle/route.js
+++ b/app/api/webhooks/paddle/route.js
@@ -152,11 +152,19 @@ export async function POST(req) {
     }
 
     if (eventType === "subscription.canceled" || eventType === "subscription.past_due") {
-      // Не чіпаємо plan/current_period_end — access_status: "blocked" вже
-      // достатньо, щоб subscription-status/route.js відрізав доступ; коли
-      // період справді закінчиться, та сама логіка обробить це так само,
-      // як і для щойно завершеного тріалу.
-      await admin.from("subscriptions").update({ access_status: "blocked" }).eq("user_id", appUser.id);
+      // ФІКС (аудит 30.08.2026, знахідка №2): раніше обидва типи події
+      // блокували доступ ОДНАКОВО і НЕГАЙНО. Для past_due (прострочена
+      // оплата) це виправдано. Але canceled — це свідома відмова людини,
+      // яка вже заплатила за поточний період: стандартна практика SaaS —
+      // залишити доступ до кінця оплаченого current_period_end, а не рвати
+      // його одразу. app/api/subscription-status/route.js вже вміє сам
+      // виставити access_status: "blocked", коли current_period_end
+      // реально мине (той самий шлях, що вже працює для завершення тріалу,
+      // periodEnded-перевірка) — тому для canceled нижче нічого не робимо,
+      // окрім past_due, де негайне блокування залишається як було.
+      if (eventType === "subscription.past_due") {
+        await admin.from("subscriptions").update({ access_status: "blocked" }).eq("user_id", appUser.id);
+      }
     }
 
     return Response.json({ ok: true });
@@ -172,13 +180,28 @@ export async function POST(req) {
   if (eventType === "transaction.completed" && addonMapping.kind === "order") {
     // Разова допуслуга (What-If) — створюємо замовлення, яке підхопить
     // app/api/cron/process-service-orders і згенерує звіт автоматично.
-    await admin.from("service_orders").insert({
-      business_id: businessId,
-      user_id: appUser.id,
-      service_type: addonMapping.service_type,
-      status: "pending",
-      paddle_transaction_id: data.id,
-    });
+    //
+    // ФІКС (аудит 30.08.2026, знахідка №3): Paddle, як і більшість платіжних
+    // провайдерів, ретраїть вебхук, якщо відповідь не прийшла вчасно/була
+    // помилкою на нашій стороні (холодний старт Vercel і т.п.). Без цієї
+    // перевірки повторна доставка того самого transaction.completed
+    // створювала ДРУГИЙ service_orders, і process-service-orders генерував
+    // і відправляв ДРУГИЙ платний звіт за одну оплату.
+    const { data: existingOrder } = await admin
+      .from("service_orders")
+      .select("id")
+      .eq("paddle_transaction_id", data.id)
+      .maybeSingle();
+
+    if (!existingOrder) {
+      await admin.from("service_orders").insert({
+        business_id: businessId,
+        user_id: appUser.id,
+        service_type: addonMapping.service_type,
+        status: "pending",
+        paddle_transaction_id: data.id,
+      });
+    }
   }
 
   if (
@@ -199,11 +222,19 @@ export async function POST(req) {
   }
 
   if (eventType === "subscription.canceled" || eventType === "subscription.past_due") {
-    await admin
-      .from("addon_subscriptions")
-      .update({ status: "expired" })
-      .eq("business_id", businessId)
-      .eq("addon_type", addonMapping.addon_type);
+    // ФІКС (аудит 30.08.2026, знахідка №2, той самий принцип, що і для
+    // основних тарифів вище): для canceled не займаємо status одразу —
+    // addon_subscriptions вже має current_period_end з останньої оплати,
+    // і app/api/cron/addon-expiry/route.js сам переведе в "expired", коли
+    // цей період реально мине. Для past_due (прострочена оплата) негайне
+    // блокування лишається як було.
+    if (eventType === "subscription.past_due") {
+      await admin
+        .from("addon_subscriptions")
+        .update({ status: "expired" })
+        .eq("business_id", businessId)
+        .eq("addon_type", addonMapping.addon_type);
+    }
   }
 
   return Response.json({ ok: true });
diff --git a/components/contact-form.tsx b/components/contact-form.tsx
index a9cd703..3af8537 100644
--- a/components/contact-form.tsx
+++ b/components/contact-form.tsx
@@ -130,13 +130,13 @@ export function ContactForm() {
                   <Label htmlFor="name">
                     {language === "UA" ? "Повне ім'я" : language === "DE" ? "Vollständiger Name" : "Full Name"}
                   </Label>
-                  <Input id="name" placeholder="Your name" value={formData.name} onChange={(e) => setFormData({...formData, name: e.target.value})} required className="bg-input border-border" />
+                  <Input id="name" placeholder={language === "UA" ? "Ваше ім'я" : language === "DE" ? "Ihr Name" : "Your name"} value={formData.name} onChange={(e) => setFormData({...formData, name: e.target.value})} required className="bg-input border-border" />
                 </div>
                 <div className="space-y-2">
                   <Label htmlFor="company">
                     {language === "UA" ? "Назва компанії" : language === "DE" ? "Firmenname" : "Company Name"}
                   </Label>
-                  <Input id="company" placeholder="Company name" value={formData.company} onChange={(e) => setFormData({...formData, company: e.target.value})} required className="bg-input border-border" />
+                  <Input id="company" placeholder={language === "UA" ? "Назва компанії" : language === "DE" ? "Firmenname" : "Company name"} value={formData.company} onChange={(e) => setFormData({...formData, company: e.target.value})} required className="bg-input border-border" />
                 </div>
                 <div className="space-y-2">
                   <Label htmlFor="email">
diff --git a/lib/alerts.mjs b/lib/alerts.mjs
index fa80dc6..3a43e9c 100644
--- a/lib/alerts.mjs
+++ b/lib/alerts.mjs
@@ -594,7 +594,18 @@ async function sendDailyReport(businessId, businessName, contact, kind, stats) {
     delivered = (await sendTelegram(contact.telegramId, message)) || delivered;
   }
   if (contact.emailEnabled && contact.email) {
-    const subject = kind === "morning" ? "RIVANT — ранковий звіт" : "RIVANT — вечірній підсумок";
+    // ФІКС: тема листа раніше була захардкоджена українською незалежно від
+    // contact.userLang — тіло листа (MORNING_REPORT_MESSAGE/EVENING_REPORT_MESSAGE
+    // вище) вже коректно локалізоване, а тема — ні. Клієнт з EN/DE отримував
+    // лист з англійським/німецьким текстом всередині, але українською темою
+    // ("RIVANT — ранковий звіт"/"RIVANT — вечірній підсумок") — саме так, як
+    // це виглядало на скріншотах у аудиті 30.08.2026.
+    const SUBJECT = {
+      UA: { morning: "RIVANT — ранковий звіт", evening: "RIVANT — вечірній підсумок" },
+      EN: { morning: "RIVANT — Morning Report", evening: "RIVANT — Evening Summary" },
+      DE: { morning: "RIVANT — Morgenbericht", evening: "RIVANT — Abendzusammenfassung" },
+    };
+    const subject = (SUBJECT[contact.userLang] || SUBJECT.EN)[kind];
     delivered = (await sendEmail(contact.email, subject, message)) || delivered;
   }
 
diff --git a/scripts/daily-reports.mjs b/scripts/daily-reports.mjs
index 24495f4..a8e5458 100644
--- a/scripts/daily-reports.mjs
+++ b/scripts/daily-reports.mjs
@@ -91,6 +91,22 @@ export async function runDailyReports() {
       // хоче другий дотик за день.
       if (business.digest_frequency === "morning_only" && kind === "evening") continue;
 
+      // ФІКС (обговорення 30.08.2026): раніше дайджест ішов БЕЗУМОВНО для
+      // кожного бізнесу з контактом, навіть якщо жодної інтеграції ще не
+      // підключено — новий користувач отримував "Revenue: $0 (margin 0%).
+      // No issues detected today." двічі на день просто тому, що завів
+      // акаунт. Це шум, а не корисна інформація: нема ще жодного джерела
+      // даних — нема що показувати. Тепер, поки в integrations немає жодного
+      // рядка для цього бізнесу, дайджест взагалі не рахуємо й не шлемо —
+      // без запису дедупу нижче, щоб щогодинний прогін перевіряв знову і
+      // почав слати одразу, як тільки з'явиться перша інтеграція (не чекаючи
+      // завтрашнього дня).
+      const { count: integrationsCount } = await admin
+        .from("integrations")
+        .select("id", { count: "exact", head: true })
+        .eq("business_id", business.id);
+      if (!integrationsCount) continue;
+
       const digestType = kind === "morning" ? "daily_digest_morning" : "daily_digest_evening";
       const today = localDateStr(tz);
 
